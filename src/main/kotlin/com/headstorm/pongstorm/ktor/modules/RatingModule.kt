package com.headstorm.pongstorm.ktor.modules

import io.ktor.application.Application
import io.ktor.routing.get
import io.ktor.routing.post
import io.ktor.routing.routing

/**
 *
 */

fun Application.ratingModule() {

    routing {
        get("/player") {

        }
        post("/routing") {

        }
    }

}
